# 📦 Shell 执行器 v2.2.0 更新文档

> 从 `v2.1.0` 升级到 `v2.2.0` 的新特性一览

## 🆕 新增功能

### 1. ⚡ 单次执行命令 `/sha`
- **用途**：一次性执行命令，不保留会话，执行完自动关闭
- **跨平台**：基于 Python 标准库 `asyncio.create_subprocess_shell`，Windows / Linux 均可使用
- **与 `/shell` 的区别**：`/shell` 维持持久化交互会话，`/sha` 只管执行一次就结束
- **用法**：`/sha <命令>`，如 `/sha ls -la`

## 🐛 修复

### 1. 修复交互检测兜底逻辑误触发
- **问题**：命令正常执行完毕后，兜底分支（连续无新输出）阈值太低（仅 0.6 秒），导致普通命令也频繁触发"请输入下一步操作"
- **修复**：
  - 兜底阈值从 3 次（0.6秒）提高到 20 次（4秒）
  - 兜底触发前先回头检查提示符标记是否已在缓存中，若存在则按正常结束处理

## 📝 升级指南

### 通过 WebUI 升级（推荐）
1. 进入 AstrBot WebUI → **插件管理**
2. 找到 `shell执行器`，点击 **...**  ，然后点击**重新安装**
3. 无需重启 AstrBot，新功能立即生效

### 手动升级
1. 下载新版 `main.py`、`metadata.yaml`（版本号 `v2.2.0`）
2. 覆盖 `AstrBot/data/plugins/shell执行器/` 目录下的对应文件
3. 重启 AstrBot
